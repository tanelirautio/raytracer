# Phase 2: Backend seam cleanup

## Tasks
- [x] Add a small backend creation point so `main.cpp` no longer directly constructs `WindowSDL`.
- [x] Keep `app::Window` as the stable interface.
- [x] Ensure font asset lookup remains valid from the app output directory.
